server:
  port: 9090

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      username: test1
      password: test1
      database: test
      authentication-database: test
      #uri: mongodb://test:test@localhost:270
  kafka:
    bootstrap-servers: localhost:29092
    consumer:
      group-id: topic01